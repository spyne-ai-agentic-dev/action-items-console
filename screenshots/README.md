# QA screenshots

Attach one screenshot per issue from `QA-REVIEW.md` / `QA-REVIEW.csv`, named by ID:

    screenshots/A1.png, A2.png, … B1.png, … C1.png

Each issue's "Screenshot ref" says which screen + element to capture. Capture on
UAT (real data is fine — this is an internal QA sheet, not an external release note).
Once a file exists, it renders inline where referenced in QA-REVIEW.md.
